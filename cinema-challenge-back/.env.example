# ============================================
# CONFIGURAÇÃO DO SERVIDOR
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# BANCO DE DADOS MONGODB
# ============================================
# Local MongoDB (requer MongoDB instalado)
MONGODB_URI=mongodb://localhost:27017/cinema-challenge

# Alternativa: MongoDB Atlas (cloud - gratuito)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/cinema-challenge?retryWrites=true&w=majority

# ============================================
# AUTENTICAÇÃO JWT
# ============================================
JWT_SECRET=sua-chave-secreta-muito-segura-aqui
JWT_EXPIRATION=7d

# ============================================
# INSTRUÇÕES DE USO
# ============================================
# 1. Copie este arquivo para .env
# 2. Substitua os valores de exemplo pelos valores reais
# 3. NUNCA commit o arquivo .env no git (já está no .gitignore)
#
# OPÇÃO 1: MongoDB Local
# - Instale MongoDB: https://www.mongodb.com/try/download/community
# - Use: MONGODB_URI=mongodb://localhost:27017/cinema-challenge
#
# OPÇÃO 2: MongoDB Atlas (Recomendado para desenvolvimento)
# - Crie conta gratuita: https://www.mongodb.com/cloud/atlas
# - Crie cluster gratuito (M0)
# - Obtenha string de conexão
# - Substitua <username> e <password> pelas suas credenciais
# - Use: MONGODB_URI=mongodb+srv://...
#
# OPÇÃO 3: Modo de Teste (Sem MongoDB)
# - Comente a linha MONGODB_URI
# - O servidor iniciará em modo smoke/test (dados em memória)
